pipeline {
    agent any

    stages {
        
        stage('Build_Project') {
            steps {
                echo 'Building whole project here!'
                sh 'make add'
                echo 'Done building'
            }
        }
    }
}
